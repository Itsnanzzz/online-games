<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Snake Game</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="/css/style.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white flex flex-col items-center justify-center min-h-screen">
    <div class="container mx-auto p-4">
        <div id="startScreen" class="flex flex-col items-center">
            <h1 class="text-3xl font-bold mb-4 text-center">Snake Game</h1>
            <button id="startButton" class="px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded text-white transition-colors">Start Game</button>
        </div>
        <div id="gameScreen" class="hidden flex flex-col items-center">
            <h1 class="text-3xl font-bold mb-4 text-center">Snake Game</h1>
            <canvas id="gameCanvas" class="mx-auto bg-gray-800 border border-gray-700 w-[300px] h-[450px] sm:w-[600px] sm:h-[400px]"></canvas>
            <div class="mt-4 text-center flex justify-center space-x-4">
                <p class="text-lg">Score: <span id="score">0</span></p>
                <p class="text-lg">Time: <span id="timer">00:00:000</span></p>
            </div>
            <p class="text-sm mt-2 text-center">Use arrow keys or swipe to control the snake</p>
            <div id="gameOverModal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden">
                <div class="bg-gray-800 p-6 rounded-lg shadow-lg text-center">
                    <h2 class="text-2xl font-bold mb-4">Game Over</h2>
                    <p class="text-lg mb-2">Your Score: <span id="finalScore">0</span></p>
                    <p class="text-lg mb-4">Time: <span id="finalTime">00:00:000</span></p>
                    <button id="okButton" class="px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded text-white transition-colors">OK</button>
                </div>
            </div>
        </div>
    </div>
    <script src="/js/snake.js"></script>
</body>
</html>